package modelo.interfaces;

public interface Consultable {

    // ── MÉTODOS ABSTRACTOS ───────────────────────────────────────────────────────
    String obtenerResumen();
    boolean estaActivo();
    String obtenerTipo();

}
